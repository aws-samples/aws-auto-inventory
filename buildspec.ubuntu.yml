version: 0.2

env:
  shell: bash

phases:
  pre_build:
    commands:
      - echo 'Install Make'
      - apt-get install -y build-essential
      - make init
  build:
    commands:
      - make build
artifacts:
  files:
    - 'dist/*'